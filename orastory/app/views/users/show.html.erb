<h1>This is your profile page <%= @user.username %></h1>

Here's the link to see your
<%= link_to 'videos' , user_videos_path(@user) %>

<%= form_for :user, url: logout_path, method: :delete do |f| %>
  <%= f.submit value: 'Log Out' %>
<% end %>
